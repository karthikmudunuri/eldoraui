---
title: Word Pull Up Text
date: 2025-01-27
description: Animated text component that pulls up words from bottom with staggered timing effect.
author: karthikmudunuri
published: true
---

<ComponentPreview name="word-pull-up-text-demo" />

## Installation

<Tabs defaultValue="cli">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
    ```bash 
    npx shadcn@latest add @eldoraui/word-pull-up-text
    ```
  </TabsContent>
  <TabsContent value="manual">
    <Steps>
      <Step>Copy and paste the following code into your project.</Step>

      `components/eldoraui/word-pull-up-text.tsx`

      <ComponentSource name="word-pull-up-text" />
      <Step>Update the import paths to match your project setup.</Step>
    </Steps>

  </TabsContent>
</Tabs>

## Usage

```tsx showLineNumbers
import { WordPullUpText } from "@/components/eldoraui/word-pull-up-text"
```

```tsx showLineNumbers
<WordPullUpText text="Word Pull Up" className="text-blue-600" />
```

## Props

| Prop        | Type     | Default | Description                                     |
| ----------- | -------- | ------- | ----------------------------------------------- |
| `text`      | `string` | `""`    | The text content to display with word animation |
| `className` | `string` | `""`    | Additional CSS classes to apply to the element  |
